spring-boot-starter-data-jdbc triggers Spring Boot to automatically attempt to configure a driver dependency in pom.xml
-> add a DB driver like in-memory DB H2

spring-boot-starter-web is needed to enable the embedded Web Application Server (Tomcat) required for hosting the Swagger UI
